version: '3.8'

services:
  node-app:
    container_name: node-app-prod
    command: yarn start
    environment:
      - PORT=${PORT}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${PORT}/health"]
      interval: 30s
      timeout: 10s
      retries: 3

